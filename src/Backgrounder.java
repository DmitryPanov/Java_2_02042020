import javax.swing.*;
import java.awt.*;

public class Backgrounder extends JPanel {

    private final Color color = new Color (
            (int)(Math.random() * 255),
            (int)(Math.random() * 255),
            (int)(Math.random() * 255)
    );

}
